fun remainder(x, y) {
    while (x >= y) {
        x -= y;
    }
    return x;
}

var begin = clock();

var result;
for (var i = 0; i < 20000; i++) {
    result = remainder(i, 5);
}
print result;

print clock() - begin;
